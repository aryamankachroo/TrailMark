# FastAPI backend
API_URL=http://localhost:8000

# Dev-mode firm (used when Clerk is not configured; maps to tmk_dev_<firm> API tokens)
TRAILMARK_DEV_FIRM=firm_demo

# Clerk (production auth — enables real sign-in + MFA when set)
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...
# CLERK_SECRET_KEY=sk_live_...
